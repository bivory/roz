{
  "name": "roz",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspaces/roz",

  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "tamasfe.even-better-toml",
        "serayuzgur.crates",
        "vadimcn.vscode-lldb",
        "streetsidesoftware.code-spell-checker",
        "DavidAnson.vscode-markdownlint"
      ],
      "settings": {
        "rust-analyzer.checkOnSave.command": "clippy",
        "editor.formatOnSave": true,
        "[rust]": {
          "editor.defaultFormatter": "rust-lang.rust-analyzer"
        },
        "files.watcherExclude": {
          "**/target/**": true
        }
      }
    }
  },

  "postCreateCommand": "/workspaces/roz/.devcontainer/setup.sh",

  "remoteEnv": {
    "RUST_LOG": "info"
  },

  "mounts": [
    "source=${localEnv:HOME}/.gitconfig,target=/home/vscode/.gitconfig,type=bind,consistency=cached",
    "source=roz-cargo-cache,target=/home/vscode/.cargo,type=volume",
    "source=roz-target-cache,target=/workspaces/roz/target,type=volume"
  ]
}
